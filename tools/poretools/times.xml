<tool id="poretools_times" name="times" version="@TOOL_VERSION@.0">
    <description>create collector's curve</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <expand macro="version_command" />
    <command detect_errors="aggressive"><![CDATA[
mkdir fast5files &&

#for $f in $fast5in
    ln -s '$f' 'fast5files/${f.name}' &&
#end for

poretools times fast5files > '$plot_out'
    ]]></command>
    <inputs>
        <param name="fast5in" type="data" format="h5" multiple="true" label="Fast5 files" />
    </inputs>
    <outputs>
        <data name="times_out" format="tabular" label="${tool.name} on ${on_string}: times"/>
    </outputs>
    <tests>
        <test>
            <param name="fast5in" value="test.fast5,test2.fast5"/>
            <output name="times_out" file="times.tab" ftype="tabular"/>
        </test>
    </tests>
    <help><![CDATA[
@HELP_GENERAL@

**Command Documentation**

List information about fast5 files

Create a collector’s curve reflecting the sequencing yield over time for a set of reads. There are two types of plots.
 The first is the yield of reads over time:, see command_doc_

.. _command_doc: http://poretools.readthedocs.io/en/latest/content/examples.html#poretools-yield-plot

    ]]></help>
    <expand macro="citations"/>
</tool>
